import { createTheme } from "@material-ui/core";

const theme = createTheme({
    palette: {
        primary: {
            main: "#bcbec0",
        },
        secondary: {
            main: "#fdb933",
        },
        text: {
            primary: "#2f3031",
            secondary: "#fdb933",
        },
    }
})

export default theme